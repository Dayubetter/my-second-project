Controller: 控制层，接收前端发送的请求，对请求进行处理，并响应数据。
service：业务逻辑层，处理具体的业务逻辑
dao：数据访问层(Data Access Object) (持久层)，负责数据访问操作，包括数据的增删查改

### 分层解耦
控制反转： Inversion of Control (IOC) 对象的创建控制权由程序自身转移到外部(容器), 这种思想成为控制反转
依赖注入： Dependency Injection (DI) 容器为应用程序提供运行时，所依赖的资源，称为依赖注入
Bean对象：IOC容器中创建、管理的对象，成为Bean