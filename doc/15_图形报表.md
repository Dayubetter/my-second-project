Echarts:一个基于js的开源可视化图表库
```mysql
-- 统计每一种职位对应的人数  group by分组后 前面分组字段+聚合函数
-- case函数 ： case 表达式 when val1 then result1 when val2 then result2 ... else ...
select
    (case job when 1 then '班主任'
        when 2 then '讲师'
        when 3 then '学工主管'
        when 4 then '教研主管'
        when 5 then '咨询师'
        else '其他' end) pos,
    count(*) num
from emp group by job order by num;

-- case函数 ： case  when 条件表达式 then result1 when 条件表达式2 then result2 ... else ...
select
    (case when job=1 then '班主任'
        when job=2 then '讲师'
        when job=3 then '学工主管'
        when job=4 then '教研主管'
        when job=5 then '咨询师'
        else '其他' end) pos,
    count(*) num
from emp group by job order by num;
```

```mysql
-- 统计员工性别人数
-- if(条件,true_value ,false_value)
select
    if(gender = 1,'男性员工','女性员工') name,
    count(*) value
from emp group by gender;
```